/* ─────────────────────────────────────────────────────────────
   PASTE YOUR TWO SUPABASE VALUES BETWEEN THE QUOTES BELOW.

   Find them in Supabase:  Project Settings → API
     • "Project URL"       → goes in SUPABASE_URL
     • "anon public" key   → goes in SUPABASE_ANON_KEY

   Both are meant to be public — they sit in the page's code.
   Nothing secret goes in this file.

   Until you fill these in, the page still works perfectly —
   it just saves on each person's own device instead of sharing.
   ───────────────────────────────────────────────────────────── */

window.SWATTI_CONFIG = {
  SUPABASE_URL:      "",
  SUPABASE_ANON_KEY: ""
};
